<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unse.bienestarestudiantil">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Vistas.LoginWelcomeActivity" />
        <activity android:name=".Vistas.LoginActivity" />
        <activity
            android:name=".Vistas.MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".Vistas.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Vistas.RegisterActivity" />
        <activity android:name=".Vistas.TransitionActivity" />


    </application>

</manifest>